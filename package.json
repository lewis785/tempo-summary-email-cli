{
  "name": "tempo-summary-email-cli",
  "version": "0.1.0",
  "description": "A command line interface to generate tempo summary emails`",
  "scripts": {
    "build": "tsc && chmod -x lib/app.js",
    "dev": "tsc --watch",
    "setup": "npm install && npm run build && npm link",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "tempo-summary-email": "lib/app.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "moment": "^2.24.0",
    "tempo-summary-email": "^0.3.2",
    "yargs": "^14.2.0"
  },
  "devDependencies": {
    "@types/node": "^12.7.12",
    "@types/yargs": "^13.0.3",
    "@typescript-eslint/eslint-plugin": "^2.4.0",
    "@typescript-eslint/parser": "^2.4.0",
    "eslint": "^6.5.1",
    "typescript": "^3.6.4"
  }
}
